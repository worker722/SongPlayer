<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SongPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="PlayButton" parent="ExoMediaButton.Play">
        <item name="android:src">@drawable/ic_baseline_play_arrow_24</item>
        <item name="android:background">@drawable/play_background</item>
    </style>

    <style name="PauseButton" parent="ExoMediaButton.Play">
        <item name="android:src">@drawable/ic_baseline_pause_24</item>
        <item name="android:background">@drawable/play_background</item>
    </style>
</resources>